import callAction from "../utils/call-action";
import { addUser, USER_MOCK_DATA } from "./user.mock";


describe("Store > User", () => {
  it("should fail if the user data is invalid", async () => {
    // callAction(addUser, [USER_MOCK_DATA]
  });
});
