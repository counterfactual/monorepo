import { RequestHandler } from "../../request-handler";
import { DepositConfirmationMessage } from "../../types";

/**
 * Place holder
 */
export default async function depositEventController(
  requestHandler: RequestHandler,
  nodeMsg: DepositConfirmationMessage
) {}
