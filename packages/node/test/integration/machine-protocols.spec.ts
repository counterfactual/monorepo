import { Node } from "../../src";

describe("Node can use the machine to execute protocols", () => {
  let node;
  beforeEach(() => {
    node = new Node();
    console.log(node);
  });

  it("can install an app", () => {});
  it("can update an app", () => {});
  it("can uninstall an app", () => {});
});
