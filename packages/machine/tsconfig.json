{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "resolveJsonModule": true,
    "outDir": "dist/"
  },
  // TODO: Figure out how to add "test" here so they get linted by tslint when using
  // the -p flag but *also* such that rollup doesn't include tests in the build.
  "include": ["src"],
  "exclude": ["dist"]
}
