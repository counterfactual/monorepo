{
  "name": "counterfactual-monorepo",
  "private": true,
  "engines": {
    "node": ">=8.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "wsrun": "wsrun",
    "build": "wsrun build $PKG --fast-exit -r --stages"
  },
  "devDependencies": {
    "lerna": "^2.11.0",
    "typescript": "^3.1.2",
    "wsrun": "^2.2.1"
  }
}
