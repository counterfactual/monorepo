#!/bin/bash
#
# A simple utility to convert the mermaid files into images.

for filename in diagrams/*.mmd; do
    [ -e "$filename" ] || continue
    mmdc -t neutral -i $filename -o build/$(basename "$filename" .mmd).svg
done