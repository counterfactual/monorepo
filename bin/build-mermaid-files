#!/bin/bash
#
# A simple utility to convert the mermaid files into images.
#
# FIXME: Gitbook.com cannot render SVG files for some reason, so we use PNG instead.

for filename in diagrams/*.mmd; do
    [ -e "$filename" ] || continue
    mmdc -t neutral -i $filename -o build/$(basename "$filename" .mmd).png
done